<%= form_for [@daily, @answer] do |answer| %>
  <%= answer.label :answer, "Subit Your Answer" %><br>
  <%= answer.text_area :answer %><br><br>

  <%= answer.label :tags, "Tags (separate by commas, use '-' with multi-word tags"%><br>
  <%= answer.text_field :tag_list %><br>

  <%= answer.submit %>
<% end %>